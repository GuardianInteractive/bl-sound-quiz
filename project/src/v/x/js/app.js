define(['js/app/audio'],
    function () {
    'use strict';

//    console.log('in app.js BEFORE setup()');

    function setupRound() {
//        console.log('in app.js INSIDE setup()');
//        Gui.setup(el);
    }

    return {
        start: setupRound
    };
});